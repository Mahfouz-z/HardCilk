#ifndef {{ factories.includeGuard }}
#define {{ factories.includeGuard }}

#include <hardcilk/Factory.hpp>

/* BEGIN include files */
{%- for x in factories.hppIncludes %}
#include {{ x }}
{%- endfor %}
/* END */

{% if factories.namespace -%}
namespace {{ factories.namespace }}
{

{% endif -%}

/** @brief Factories generated by the bulk script. */
extern hardcilk::Factory {{ factories.variableName }}[{{ factories|length }}];

{% if factories.namespace -%}
} /* namespace {{ factories.namespace }} */

{% endif -%}

#endif // {{ factories.includeGuard }}
